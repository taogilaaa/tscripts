{
  "packages": [
    "packages/*"
  ],
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": "main",
      "message": "chore(release): publish",
      "ignoreChanges": [
        "**/*.md",
        "**/__tests__/**"
      ]
    }
  }
}
